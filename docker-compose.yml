services:
  dev:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
    ports:
      - "5173:5173"
    volumes:
      - .:/workspaces/bryanvarela.dev
      - /workspaces/bryanvarela.dev/node_modules
    command: npm run dev -- --host
    restart: unless-stopped
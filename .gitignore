node_modules/
dist/
.claude/
*.zip

# Dev container persistent home (credentials, OAuth tokens, MCP config, SSH keys)
# NEVER remove this line - contains secrets
.devcontainer/home/

# Environment & secrets
.env
.env.*

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
.playwright-mcp/

# MCP tooling config
.mcp.json
.vscode/

# Memory MCP
memory.jsonl
.ai-terminal-images/
ai_terminal_images/
.env

# Claude Code tooling (local only, no subir a GitHub)
CLAUDE.md
skills-lock.json

# Artefactos de deploy antiguos (migrado a Cloudflare Pages)
Dockerfile
.dockerignore
docker-compose.yml
CNAME

# Build-time output (regenerated on every build)
public/images/

# Local review/audit screenshots (do not commit)
review-*.png

# Build-time output (regenerated on every build by npm run build)
src/data/recent-commits.js